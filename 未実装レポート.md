全データ収集完了。監査レポートを作成します。

---

# 実装監査レポート — NovaLoop vs 競合3サービス
## 監査日: 2026-03-29 | 監査対象: `C:\Users\ram_n\Desktop\EmotionaL\NovaLoop\my-app`

---

## 1. サマリー

**競合カバー率の概要（Premiere拡張除外）:**
- **bill change** 対比: 約 **82%** — 請求/支払いの主要フローはほぼ実装済みだが、freee互換CSV・全銀正式フォーマットが未接続
- **HEGELOOP** 対比: 約 **58%** — 案件管理・進行は強いが、**動画レビュー（タイムコードコメント）・ChatWork連携・PWA・修正ラウンド管理** が完全欠落
- **案件管理シートPro** 対比: 約 **68%** — カレンダー/ガントは実装済みだが、スプレッドシート的な一覧インライン編集・負荷可視化・ChatWork通知が欠落

**一番危ない未実装領域:**
> **動画レビュー（タイムコード付きコメント・修正ラウンド管理）** — HEGELOOPの中核機能であり、コードベース上にほぼゼロの実装。これ1点でSNS動画制作現場でのHEGELOOP置換が不可能。

**総合カバー率（Premiere拡張除外・厳格判定）: 約 65%**

---

## 2. ルート/データモデル/主要機能の全体把握

### 主要ページルート（確認済み）

| ルート | 説明 |
|--------|------|
| `/contents` | コンテンツ(案件)一覧・進行管理 |
| `/projects`, `/projects/[id]` | プロジェクト管理（9タブ: 概要/コンテンツ/タスク/カレンダー/素材/変更/財務/連携/ログ） |
| `/billing` | 月次請求生成（ワンクリック） |
| `/invoices`, `/invoices/new`, `/invoices/[id]` | 請求書CRUD + PDF |
| `/payouts` | 外注支払い + CSV(全銀)エクスポート |
| `/vendors`, `/vendors/[id]` | 外注先管理 |
| `/vendors/submissions` | 外注請求書提出管理 |
| `/vendor-submit/[token]` | 外注向けポータル（認証不要） |
| `/pages`, `/pages/[id]` | Notion風ページエディタ |
| `/calendar` | カレンダー + ICSエクスポート + Google Calendar連携 |
| `/timeline` | ガントチャート（依存関係・工数） |
| `/finance-lite` | 収支/粗利ダッシュボード |
| `/materials` | 素材/成果物管理 |
| `/changes` | 変更依頼管理 |
| `/exceptions` | 例外/アラート管理 |
| `/notifications` | 通知センター |
| `/members` | メンバー管理 |
| `/settings/*` | 各種設定（members/roles/export/import/audit/ai-channels/workspace 等） |
| `/onboarding` | 初回セットアップ |
| `/invite` | 招待受諾 |
| `/join-request` | 参加申請 |
| `/platform/*` | ライセンス管理（プラットフォーム側） |

### 主要テーブル/スキーマ（SQL確認済み）

| テーブル | 説明 |
|----------|------|
| `organizations` | 組織 |
| `app_users` | ユーザー（role: owner/executive_assistant/pm/director/worker） |
| `org_roles` | カスタムロール（UI上は動くがRLSは未適用） |
| `clients` | 請求先クライアント（法人/個人） |
| `contents` | コンテンツ/案件（due_editor_at, due_client_at, billable_flag, delivery_month, links_json, health_score, unit_price等） |
| `invoices` + `invoice_lines` | 請求書 + 明細 |
| `vendors` + `vendor_invoices` + `vendor_invoice_lines` | 外注先 + 外注請求書 |
| `payouts` | 支払い記録 |
| `payout_csv_exports` | CSV出力履歴 |
| `projects` | プロジェクト（chatwork_room_id, google_calendar_id, slack_channel_id, discord_channel_id フィールド有り） |
| `project_tasks` | タスク（依存関係・工数ポイント） |
| `schedule_events` | カレンダーイベント |
| `material_assets` | 素材/成果物（バージョン管理） |
| `change_requests` | 変更依頼 |
| `expenses` | 経費 |
| `exceptions` | 例外/アラート |
| `pages` + `page_revisions` + `page_comments` | Notionページ + 履歴 + コメント |
| `notifications` | 通知 |
| `audit_logs` | 監査ログ（36アクション種別） |
| `export_jobs` | エクスポートジョブ |
| `content_templates` | コンテンツテンプレート |
| `workflow_templates` | ワークフローテンプレート（Beta） |
| `external_channel_links` | Discord/LINE連携 |
| `org_bank_accounts` | 複数銀行口座 |

### 主要API（確認済み・112エンドポイント）

請求: `/api/billing/generate` `/api/billing/bulk-create` `/api/invoices/bulk-zip` `/api/payouts/csv-export`  
外注: `/api/vendors/*` `/api/vendor-submissions/*` `/api/vendor-submit/*`  
ページ: `/api/pages/*`（CRUD + revisions + comments + templates）  
通知: `/api/notifications/*`  
メンバー: `/api/org/members` `/api/org/invites/*` `/api/org/requests/*`  
AIチャット: `/api/ai/external/chat` (Discord/LINE)  
エクスポート: `/api/exports/*` `/api/imports/*`

---

## 3. 競合機能比較表

### A. HEGELOOP 由来の機能

| 機能カテゴリ | 機能名 | 競合名 | 判定 | 根拠ファイル | 根拠要約 | 備考 |
|---|---|---|---|---|---|---|
| A-1 認証・初期導線 | Googleログイン | HEGELOOP | 実装済み | `app/auth/finish/page.tsx`, `@supabase/ssr` | Supabase Auth経由でGoogle OAuth。auth/finish でセッション確認 | |
| A-1 | サインアップ/ログイン | HEGELOOP | 実装済み | `app/auth/finish/page.tsx` | セッション確認・リダイレクト実装 | |
| A-1 | 初回導線 | HEGELOOP | 実装済み | `app/onboarding/page.tsx`, `lib/onboarding.ts` | 8ステップのオンボーディングチェックリスト | |
| A-1 | チーム/組織への参加 | HEGELOOP | 実装済み | `app/invite/page.tsx`, `app/join-request/page.tsx`, `app/api/org/invites/accept/route.ts` | 招待URLと参加申請の両フロー実装 | |
| A-2 チーム/組織 | チーム作成 | HEGELOOP | 実装済み | `app/api/orgs/create/route.ts`, `app/api/onboarding/create-org/route.ts` | API + DBへの永続化確認 | |
| A-2 | 招待コード/招待相当 | HEGELOOP | 実装済み | `app/api/org/invites/create/route.ts`, `app/invite/page.tsx` | 招待メール + トークン方式 | |
| A-2 | チーム切替 | HEGELOOP | 実装済み | `hooks/useAuthOrg.ts`, `app/api/auth/my-orgs/route.ts` | 複数org取得→切替フック実装 | |
| A-2 | チーム退出 | HEGELOOP | 実装済み | `app/api/orgs/leave/route.ts` | APIエンドポイント確認 | |
| A-2 | 複数チーム/組織の切替利用 | HEGELOOP | 実装済み | `app/api/auth/my-orgs/route.ts`, `hooks/useAuthOrg.ts` | 複数org対応のhook | |
| A-3 メンバー管理 | メンバー一覧 | HEGELOOP | 実装済み | `app/settings/members/page.tsx`, `app/members/page.tsx` | 2種のUI実装（設定画面 + 専用ページ） | |
| A-3 | メンバー追加 | HEGELOOP | 実装済み | `app/api/org/invites/create/route.ts` | 招待フロー完備 | |
| A-3 | 役割管理（編集者/ディレクター等） | HEGELOOP | 部分実装 | `supabase/sql/029_org_roles_and_role_id.sql`, `app/settings/roles/page.tsx` | UIとDBはある。**RLSは依然としてowner/executive_assistantしか見ていない**。カスタムロールは表示のみ | 致命的弱点 |
| A-3 | メンバー並び替え | HEGELOOP | 未実装 | （コードなし） | メンバーのsort_order列・並び替えUI確認できず | |
| A-3 | メンバー編集/削除 | HEGELOOP | 実装済み | `app/api/org/members/update-role/route.ts`, `app/settings/members/page.tsx` | ロール変更・削除確認 | |
| A-3 | ChatWork由来メンバー情報取込 | HEGELOOP | 未実装 | （コードなし） | ChatWork連携自体が未実装 | |
| A-4 案件テンプレート | テンプレート作成 | HEGELOOP | 部分実装 | `supabase/sql/011_content_templates.sql`, `lib/contentsGuidedCatalog.ts` | DBテーブル有り、ガイデッドカタログ定義有り。UIの作成フォームが独立画面かは未確認 | |
| A-4 | テンプレート一覧 | HEGELOOP | 部分実装 | `lib/contentsGuidedCatalog.ts` | カタログ定義は存在するがブラウザ上のテンプレ一覧画面が未確認 | |
| A-4 | テンプレ編集/削除 | HEGELOOP | 未確認 | `supabase/sql/011_content_templates.sql` | DBにはCRUD可能。UIの編集/削除フロー未確認 | |
| A-4 | テンプレ順序管理 | HEGELOOP | 未確認 | `supabase/sql/011_content_templates.sql` | sort_orderカラムあり。UI確認できず | |
| A-4 | テンプレから案件起票 | HEGELOOP | 部分実装 | `lib/contentsGuidedCatalog.ts` | Guided catalog経由の導線は存在。UIの完全な接続は未確認 | |
| A-5 制作進行/案件作成 | 新規案件作成 | HEGELOOP | 実装済み | `app/contents/page.tsx` | フル実装のコンテンツ作成UI | |
| A-5 | 案件名/projectタイトル | HEGELOOP | 実装済み | `contents`テーブル `title`列, `app/contents/page.tsx` | | |
| A-5 | 担当編集者アサイン | HEGELOOP | 実装済み | `contents`テーブル `assignee_user_id`, `app/contents/page.tsx` | | |
| A-5 | 確認者/ディレクターアサイン | HEGELOOP | 部分実装 | `content_assignments`テーブル | content_assignmentsで複数アサイン対応。UI上での「確認者」明示確認できず | |
| A-5 | 素材リンク登録 | HEGELOOP | 実装済み | `contents.links_json`, `app/contents/page.tsx` | JSON配列でリンク管理 | |
| A-5 | 複数素材リンク | HEGELOOP | 実装済み | `contents.links_json` | JSON配列で複数対応 | |
| A-5 | 素材共有日/初稿日/提出期限 | HEGELOOP | 実装済み | `contents.due_editor_at`, `contents.due_client_at` | 2つの日付フィールド実装 | |
| A-5 | 提出/初稿/納品リンク管理 | HEGELOOP | 実装済み | `contents.links_json`, `material_assets`テーブル | links_json + 素材管理テーブルで対応 | |
| A-5 | 一覧での管理 | HEGELOOP | 実装済み | `app/contents/page.tsx` | テーブル/カード2ビュー + 複合フィルタ | |
| A-6 ステータス/進捗管理 | ステータス一覧 | HEGELOOP | 実装済み | `lib/contentWorkflow.ts` | 13ステータス定義（not_started→published/canceled） | |
| A-6 | チームごとの独自ステータス定義 | HEGELOOP | 部分実装 | `supabase/sql/057_work_items_beta.sql` `workflow_templates`テーブル | DBテーブルは存在。UIからの設定フロー未確認 | |
| A-6 | ステータス追加/削除/並び替え | HEGELOOP | 部分実装 | `workflow_templates`テーブル | DB層あり。管理UIの確認できず | |
| A-6 | 進捗項目（カット完了等） | HEGELOOP | 部分実装 | `contents`の`material_status`, `draft_status`, `final_status` | ステータスグループ有り。「カット完了」等の細粒度チェックは未確認 | |
| A-6 | 進捗の可視化 | HEGELOOP | 実装済み | `app/contents/page.tsx`, `app/projects/[id]/page.tsx` | ヘルススコア・ステータスカラム可視化あり | |
| A-6 | 編集者側の進行状況確認 | HEGELOOP | 部分実装 | （役割ベースのビュー未確認） | コンテンツ一覧は共通。「編集者専用ビュー」の分離未確認 | |
| A-7 レビュー/修正指示 | **動画レビュー画面** | HEGELOOP | **未実装** | （コードなし） | 動画プレイヤー埋め込みコンポーネント・ビューア画面の存在なし | **最大ギャップ** |
| A-7 | **タイムコード付き修正コメント** | HEGELOOP | **未実装** | `lib/pageTemplateCatalog.ts:886` (テンプレ名のみ) | "timecode-snippets"というテンプレ名が1件あるのみ。実装ゼロ | |
| A-7 | 動画プレイヤーから秒数選択 | HEGELOOP | **未実装** | （コードなし） | | |
| A-7 | 修正文言テンプレート | HEGELOOP | **未実装** | （コードなし） | AIPaletteのsend_messageモードはあるが修正指示専用テンプレなし | |
| A-7 | 編集者向け共有リンク発行 | HEGELOOP | 未確認 | `vendor-submit/[token]` | 外注ポータルは存在。「編集者向け」の独立した共有リンクは未確認 | |
| A-7 | 編集者が修正チェックできる | HEGELOOP | **未実装** | （コードなし） | | |
| A-7 | リアルタイム反映 | HEGELOOP | **未実装** | （コードなし） | Supabase realtimeの使用確認できず | |
| A-7 | ディレクター側で消化状況確認 | HEGELOOP | **未実装** | （コードなし） | | |
| A-7 | コメント返信/備考 | HEGELOOP | 部分実装 | `page_comments`テーブル, `app/api/pages/[id]/comments/route.ts` | **ページ上**のコメントは実装済み。コンテンツ/案件単位のコメントは未確認 | |
| A-7 | **修正ラウンド管理** | HEGELOOP | **未実装** | （コードなし） | 初稿→修正1→修正2の管理構造なし。revision_countフィールドはあるが管理UIなし | |
| A-7 | 過去ラウンドの参照 | HEGELOOP | **未実装** | （コードなし） | | |
| A-8 モバイル/PWA | スマホから閲覧 | HEGELOOP | 部分実装 | Tailwind CSS使用 | レスポンシブCSS前提の設計。PWA固有対応なし | |
| A-8 | 共有リンクのスマホ確認 | HEGELOOP | 部分実装 | `app/vendor-submit/[token]` | 外注ポータルはモバイル対応想定。動画レビューのスマホ確認は未実装 | |
| A-8 | **PWA相当（manifest等）** | HEGELOOP | **未実装** | `public/`ディレクトリ確認済み | manifest.jsonなし・Service Worker設定なし | |
| A-8 | モバイルでレビュー確認 | HEGELOOP | **未実装** | （動画レビュー自体が未実装） | | |
| A-9 通知/連携 | **ChatWork連携** | HEGELOOP | **未実装** | `projects.chatwork_room_id`フィールドのみ | フィールド定義だけ。APIクライアント・メッセージ送信・Webhook処理ゼロ | **P0ギャップ** |
| A-9 | APIトークン保存 | HEGELOOP | **未実装** | （ChatWork連携なし） | | |
| A-9 | デフォルト送信先ルーム設定 | HEGELOOP | **未実装** | （ChatWork連携なし） | | |
| A-9 | 案件ごとの送信先設定 | HEGELOOP | **未実装** | （ChatWork連携なし） | | |
| A-9 | 定時自動リマインド | HEGELOOP | 部分実装 | `app/api/notifications/digest/route.ts`, `app/api/invoice-requests/reminders/route.ts` | リマインダーAPIはあるが、Cronジョブ/スケジュール実行の接続確認できず | |
| A-9 | **Lark連携** | HEGELOOP | **未実装** | （コードなし） | コードベース全体でLark文字列ゼロ | |
| A-9 | 通知メッセージ生成/コピー | HEGELOOP | 実装済み | `components/AIPalette.tsx`（send_messageモード） | AI経由でメッセージ生成・コピーUI実装 | |
| A-10 プラン/権限制御 | 機能ごとのプラン制御 | HEGELOOP | 実装済み | `supabase/sql/061_062_platform_creator_license.sql`, `lib/platform.ts` | ライセンス/エンタイトルメントシステム実装 | |
| A-10 | **ロール/プランで操作制限** | HEGELOOP | **部分実装** | `app/settings/roles/page.tsx`コメント | 「Custom roles are UI-only, RLS still uses owner/executive_assistant」と明記 | RLS未適用は致命的 |
| A-10 | 無料/有料での機能差分管理 | HEGELOOP | 実装済み | `app/platform/entitlements/page.tsx`, `app/api/platform/entitlements/*` | エンタイトルメント付与/取消API実装 | |

### B. bill change 由来の機能

| 機能カテゴリ | 機能名 | 競合名 | 判定 | 根拠ファイル | 根拠要約 | 備考 |
|---|---|---|---|---|---|---|
| B-1 請求書発行 | 請求書作成 | bill change | 実装済み | `app/invoices/new/page.tsx`, `app/api/invoices/create/route.ts` | フル実装 | |
| B-1 | 請求書一覧 | bill change | 実装済み | `app/invoices/page.tsx` | 検索・月・ステータスフィルタ完備 | |
| B-1 | 請求書詳細 | bill change | 実装済み | `app/invoices/[id]/page.tsx` | | |
| B-1 | 請求書複製 | bill change | 実装済み | `app/api/invoices/[id]/copy/route.ts` | APIエンドポイント確認 | |
| B-1 | PDF出力 | bill change | 実装済み | `app/api/invoices/[id]/generate-pdf/route.ts`, `app/api/invoices/bulk-zip/route.ts` | Puppeteer+Chromiumで生成。Supabase Storageへ保存 | |
| B-1 | 請求ステータス管理 | bill change | 実装済み | `invoices.status`（draft/issued/void） | 3ステータス | |
| B-1 | 請求先管理 | bill change | 実装済み | `clients`テーブル, `supabase/sql/043_bill_change_gap_close.sql` | billing_name, billing_email, address対応 | |
| B-1 | 発行日/支払期限管理 | bill change | 実装済み | `invoices.issue_date`, `invoices.due_date` | | |
| B-1 | 明細行管理 | bill change | 実装済み | `invoice_lines`テーブル | qty, unit_price, amount, description | |
| B-1 | 一括PDF出力 | bill change | 実装済み | `app/api/invoices/bulk-zip/route.ts` | ZIPで複数PDF出力 | |
| B-1 | ZIP出力 | bill change | 実装済み | `app/api/invoices/bulk-zip/route.ts` | バイナリZIPレスポンス | |
| B-2 受領請求/支払い管理 | 外注請求書/受領請求登録 | bill change | 実装済み | `vendor_invoices`テーブル, `app/vendors/page.tsx` | | |
| B-2 | 外注請求一覧 | bill change | 実装済み | `app/vendors/page.tsx`, `app/vendors/submissions/page.tsx` | | |
| B-2 | 外注請求ステータス管理 | bill change | 実装済み | `vendor_invoices.status`（draft/submitted/approved/rejected/paid） | 5ステータス | |
| B-2 | 支払い記録 | bill change | 実装済み | `payouts`テーブル, `app/payouts/page.tsx` | | |
| B-2 | 支払予定/支払済み | bill change | 実装済み | `vendor_invoices.pay_date`, `payouts`テーブル | | |
| B-2 | 証憑保存 | bill change | 部分実装 | `app/api/vendor-invoices/[id]/pdf/route.ts` | PDF保存はあり。証憑（レシート/領収書）の独立した添付機能は未確認 | |
| B-2 | 月次支払い生成 | bill change | 実装済み | `app/api/vendors/generate-monthly/route.ts` | | |
| B-2 | 外注請求から支払いへの接続 | bill change | 実装済み | `payouts`テーブル + `vendor_invoices.status=paid` | フロー接続確認 | |
| B-3 依頼/回収フロー | 請求依頼 | bill change | 実装済み | `app/api/vendors/request-invoice/route.ts` | | |
| B-3 | 一括請求依頼 | bill change | 部分実装 | `app/api/invoices/bulk-send-prep/route.ts` | クライアント側の一括送信準備あり。外注への一括依頼は未確認 | |
| B-3 | 外注への請求回収導線 | bill change | 実装済み | `app/vendor-submit/[token]`, `app/api/vendor-submit/*` | トークン認証付き外注ポータル | |
| B-3 | フォーム/アップロード/CSV経由回収 | bill change | 部分実装 | `app/vendor-submit/[token]`, `app/api/vendor-submissions/route.ts` | フォーム提出あり。CSV回収は未確認 | |
| B-3 | 回収状況確認 | bill change | 実装済み | `app/vendors/submissions/page.tsx` | 提出状況一覧 | |
| B-4 会計/銀行連携 | CSVエクスポート | bill change | 実装済み | `app/api/payouts/csv-export/route.ts` | zengin_simple / custom_basic フォーマット | |
| B-4 | **freee連携/freee取込前提CSV** | bill change | **未実装** | docs/spec.md, AGENTS.md（設計文書のみ） | freeeとの実際の連携コードゼロ。ドキュメントのみ | **P1ギャップ** |
| B-4 | **全銀フォーマットCSV正式対応** | bill change | **部分実装** | `app/api/payouts/csv-export/route.ts`（zengin_simpleフォーマット） | "zengin_simple"という独自フォーマット。正式全銀フォーマット（固定長）かどうか未確認 | |
| B-4 | 銀行振込向けデータ出力 | bill change | 実装済み | `app/api/payouts/csv-export/route.ts`, `org_bank_accounts`テーブル | CSV出力 + 銀行口座管理あり | |
| B-5 組織/権限 | 社員招待 | bill change | 実装済み | `app/api/org/invites/create/route.ts` | | |
| B-5 | 税理士招待相当 | bill change | 部分実装 | `org_roles`テーブル | カスタムロール定義はできる。RLSが未適用なため実効性なし | |
| B-5 | 権限分離 | bill change | 部分実装 | `app/settings/roles/page.tsx`（UI-only） | 前述の通りRLS未適用 | |
| B-5 | 経理系画面のアクセス制御 | bill change | 部分実装 | `app/projects/page.tsx`（`canViewFinance`フラグ） | フロント側でのcanViewFinanceロジックあり。RLS側の担保なし | |
| B-6 一括処理/運用性 | 一括ステータス変更 | bill change | 実装済み | `app/api/invoices/bulk-update/route.ts`, `app/api/payouts/bulk-update/route.ts` | | |
| B-6 | 一括出力 | bill change | 実装済み | `app/api/invoices/bulk-zip/route.ts`, `app/api/vendor-invoices/bulk-zip/route.ts` | | |
| B-6 | 月次バッチ処理 | bill change | 部分実装 | `app/api/billing/generate/route.ts`, `app/api/billing/bulk-create/route.ts` | UIトリガーのバッチ。Cronジョブ/自動実行の接続未確認 | |
| B-6 | 大量件数前提のジョブ処理 | bill change | 部分実装 | `export_jobs`テーブル, `app/api/exports/*` | export_jobsテーブルあり。バックエンドの非同期プロセッサ確認できず | |
| B-6 | 監査ログ | bill change | 実装済み | `app/settings/audit/page.tsx`, `lib/auditLog.ts`, `supabase/sql/036_audit_logs.sql` | 36アクション種別・フル実装 | |

### C. 案件管理シートPro 由来の機能

| 機能カテゴリ | 機能名 | 競合名 | 判定 | 根拠ファイル | 根拠要約 | 備考 |
|---|---|---|---|---|---|---|
| C-1 案件管理の柔軟性 | スプレッドシート的な一覧性 | シートPro | 部分実装 | `app/contents/page.tsx` | テーブルビューあり。セル直接編集・コピペ・列リサイズ等は未確認 | |
| C-1 | 絞り込み/検索 | シートPro | 実装済み | `app/contents/page.tsx` | 複合フィルタ（クライアント/プロジェクト/ステータス/ヘルス/カテゴリ）+ 全文検索 | |
| C-1 | 並び替え | シートPro | 部分実装 | `app/contents/page.tsx` | フィルタは豊富だが、列クリックでのソートは未確認 | |
| C-1 | 一覧での高速編集（インライン編集） | シートPro | **部分実装** | `app/contents/page.tsx` | 行展開での編集はあり。セル直接編集（スプレッドシート的な操作）は未確認 | |
| C-1 | 自由な項目追加/削除に近い柔軟性 | シートPro | **未実装** | （固定スキーマ） | スキーマはSQLで固定。カスタムフィールドの追加機能なし | |
| C-1 | コピペ運用に強い導線 | シートPro | **未実装** | （コードなし） | クリップボード操作やバルクペースト機能の証拠なし | |
| C-1 | テンプレート化 | シートPro | 部分実装 | `content_templates`テーブル, `lib/contentsGuidedCatalog.ts` | DBとロジック定義はあり。完全なUIフローは未確認 | |
| C-2 スケジュール/可視化 | ガントチャート相当 | シートPro | 実装済み | `app/timeline/page.tsx`, `project_tasks`テーブル | タスク依存・工数・今日インジケーター完備 | |
| C-2 | カレンダー相当 | シートPro | 実装済み | `app/calendar/page.tsx`, `schedule_events`テーブル | Day/Week/Month切替 + ICS/Google Calendar連携 | |
| C-2 | 納期の可視化 | シートPro | 実装済み | `app/contents/page.tsx`, `app/timeline/page.tsx` | 遅延ハイライト・due_editor_at/due_client_at表示 | |
| C-2 | **担当ごとの負荷/稼働可視化** | シートPro | **部分実装** | `project_tasks.workload_points`, `app/timeline/page.tsx` | 工数ポイントはあるが、担当者別の負荷チャート/リソース管理ビューは未確認 | |
| C-2 | 週次/日次の視点切替 | シートPro | 実装済み | `app/calendar/page.tsx` | Day/Week/Month切替実装 | |
| C-3 収支管理 | 売上集計 | シートPro | 実装済み | `app/projects/page.tsx`, `app/finance-lite/page.tsx` | プロジェクト別月次売上集計 | |
| C-3 | 外注費集計 | シートPro | 実装済み | `app/finance-lite/page.tsx` | vendor cost集計あり | |
| C-3 | 利益/粗利の可視化 | シートPro | 実装済み | `app/finance-lite/page.tsx` | 粗利・マージン率・低マージン検知まで実装 | |
| C-3 | 月次集計 | シートPro | 実装済み | `app/finance-lite/page.tsx` | 月次トレンド実装 | |
| C-3 | グラフ表示 | シートPro | 部分実装 | `app/finance-lite/page.tsx` | 財務サマリーカード・トレンドリスト表示。インタラクティブグラフ（Chart.js等）の依存なし | |
| C-4 案件運用 | 案件ステータス管理 | シートPro | 実装済み | `lib/contentWorkflow.ts`, `app/contents/page.tsx` | 13ステータス完備 | |
| C-4 | 素材/修正稿/成果物のリンク管理 | シートPro | 実装済み | `material_assets`テーブル, `app/materials/page.tsx` | バージョン管理付き | |
| C-4 | 案件検索 | シートPro | 実装済み | `app/contents/page.tsx` | 複合フィルタ+検索 | |
| C-4 | 案件の横断閲覧 | シートPro | 実装済み | `app/contents/page.tsx` | プロジェクト横断の全コンテンツ表示 | |
| C-4 | 担当者別管理 | シートPro | 実装済み | `app/contents/page.tsx` | assignee_user_idでのフィルタ実装 | |
| C-5 外部通知/連携 | **ChatWork通知** | シートPro | **未実装** | `projects.chatwork_room_id`のみ | フィールドのみ。API呼び出しゼロ | |
| C-5 | 案件進行に応じた通知 | シートPro | 部分実装 | `lib/notifications.ts`, `app/api/notifications/digest/route.ts` | 20通知タイプ・通知センター実装。プッシュ通知は未実装 | |
| C-5 | Slack/Discord/カレンダー等の連携余地 | シートPro | 部分実装 | `projects.slack_channel_id`, `projects.discord_channel_id`フィールド, `external_channel_links` | フィールドとDiscord AIチャンネルはあり。Slackへのメッセージ送信は未実装 | |
| C-5 | 通知設計の土台 | シートPro | 実装済み | `lib/notifications.ts`, `notifications`テーブル | 20種別・優先度・既読/未読・解決管理の完全実装 | |
| C-6 SaaS置換安心機能 | export | シートPro | 実装済み | `app/settings/export/page.tsx`, `app/api/exports/*`, `export_jobs`テーブル | ジョブ管理 + 署名URL発行 | |
| C-6 | import | シートPro | 実装済み | `app/settings/import/page.tsx`, `app/api/imports/*` | プレビュー+適用フロー。PDF/ファイルは対象外明記 | |
| C-6 | assets移行 | シートPro | 部分実装 | `app/settings/assets/page.tsx` | ページは存在。実装の深さは未確認 | |
| C-6 | データの持ち出し | シートPro | 実装済み | export実装 + PDF ZIP | | |
| C-6 | 監査/履歴 | シートPro | 実装済み | `app/settings/audit/page.tsx`, `audit_logs`テーブル | フル実装 | |
| C-6 | 権限管理 | シートPro | 部分実装 | `org_roles` + RLS未適用問題 | UI-onlyのため実効性に疑問 | |
| C-6 | バックアップ/復元相当 | シートPro | 未実装 | （コードなし） | exportはあるがバックアップ自動化・復元フローなし | |

### D. このプロダクト独自の機能

| 機能カテゴリ | 機能名 | 判定 | 根拠ファイル | 根拠要約 |
|---|---|---|---|---|
| D-1 /contents | 1行=1コンテンツ管理 | 実装済み | `app/contents/page.tsx` | |
| D-1 | due_client_at / due_editor_at | 実装済み | `contents`テーブル | |
| D-1 | status管理（13種） | 実装済み | `lib/contentWorkflow.ts` | |
| D-1 | thumbnail_done / unit_price / billable_flag / delivery_month / links_json | 実装済み | `contents`テーブル | |
| D-1 | 今日/明日の提出集計・遅延集計 | 実装済み | `components/Sidebar.tsx`, `lib/contentWorkflow.ts` | サイドバーで常時集計表示 |
| D-2 /billing /invoices | delivery_month起点の請求生成 | 実装済み | `lib/monthlyBilling.ts`, `app/api/billing/generate/route.ts` | |
| D-2 | client別グルーピング | 実装済み | `lib/monthlyBilling.ts` | |
| D-2 | invoice draft作成 + PDF保管 | 実装済み | `app/api/invoices/*`, Supabase Storage | |
| D-2 | 請求条件設定 | 実装済み | `org_settings`テーブル, `app/api/org-settings/route.ts` | |
| D-2 | 法人/個人分岐 | 実装済み | `clients.entity_type`（corporate/individual） | |
| D-3 /vendors /payouts | 外注請求登録 + CSV取込 + 承認 + payout生成 + 支払日設定 + 証憑保存 | 実装済み | `app/vendors/page.tsx`, `app/payouts/page.tsx` | |
| D-4 /pages | Notion風ページ + リッチテキスト + 画像/embed + 並び替え + 権限制御 | 実装済み | `app/pages/[id]/page.tsx`（Tiptap完全実装） | |
| D-5 /notifications | 参加申請/承認/却下/遅延/締め通知 + 既読/未読 + フィルタ | 実装済み | `lib/notifications.ts`, `app/notifications/page.tsx` | |
| D-6 /settings | members/roles/export/import/assets/audit/health | 実装済み（rolesはUI-only） | `app/settings/*/page.tsx` | |

---

## 4. 実装済みだが弱い機能

| 機能 | なぜ弱いか | 何が足りないか |
|---|---|---|
| **カスタムロール/権限管理** | UIで設定できるが、RLSは依然owner/executive_assistantしか見ていない。フロント側フラグのみ | RLS PolicyをCustom Role対応に書き換える必要あり |
| **全銀CSVエクスポート** | "zengin_simple"という独自フォーマット。正式な全銀フォーマット（固定長・JIS X 0208）かどうか不明 | 正式全銀フォーマットの検証・実装 |
| **定時自動リマインド** | APIエンドポイント（/digest, /reminders）は存在するが、Cron/スケジュール起動の接続が確認できない | Vercel Cron等でのジョブ登録 |
| **大量件数エクスポート** | export_jobsテーブルはあるが、非同期バックエンドプロセッサの実装が確認できない | バックグラウンドジョブ処理の実装 |
| **コンテンツテンプレート** | DBとロジックはあるが、管理UIのフロー（作成/編集/削除/一覧/テンプレから起票）が独立した完全な画面として確認できない | テンプレート管理の専用UI |
| **グラフ/チャート表示** | finance-liteにリスト表示はあるが、Chart.js等のグラフライブラリを未導入 | ビジュアル化のためのライブラリ追加 |
| **担当者負荷可視化** | workload_pointsはDBにあるが、人別リソース配分チャートがない | リソース配分ビューの実装 |
| **Slack連携** | slack_channel_idフィールドはあるが、実際のメッセージ送信コードなし | Slack Webhook/API統合 |
| **外注請求書の証憑添付** | PDF保管はあるが、領収書/発行原本の独立した証憑管理機能が弱い | 証憑専用のファイル管理フロー |

---

## 5. 未実装/部分実装のうち、競合を潰すのに必須なもの

### P0 — 今すぐ埋めないと競合から置換できない機能

| # | 機能 | 対象競合 | 現状 |
|---|---|---|---|
| 1 | **動画レビュー（タイムコードコメント・修正ラウンド管理）** | HEGELOOP | 完全未実装。SNS動画制作の現場でHEGELOOPを置換する際の最大障壁 |
| 2 | **ChatWork連携（APIトークン保存・ルーム送信・自動リマインド）** | HEGELOOP / シートPro | フィールドのみ。SNS運用代行現場のデファクト通知ツールへの接続がゼロ |
| 3 | **カスタムロールのRLS実際適用** | HEGELOOP / bill change | UIで設定した権限が実際にDBレベルで機能していない。税理士・外部ディレクター等の権限分離が不可能 |

### P1 — 近いうちに必要な機能

| # | 機能 | 対象競合 | 現状 |
|---|---|---|---|
| 4 | **PWA/モバイル最適化（manifest + viewport対応）** | HEGELOOP | 動画チェックを外出先でやる現場では必須。manifest.jsonゼロ |
| 5 | **freee互換CSVエクスポート（または正式全銀フォーマット）** | bill change | 経理との分業を考えると「うちのCSVはfreeeに入れられます」は販売トーク上必須 |
| 6 | **定時自動リマインド（Cron接続）** | HEGELOOP / シートPro | digestとreminderのAPIはあるがスケジュール実行されない。納期アラートが機能しない |
| 7 | **修正ラウンド管理（初稿→修正N回の明確な区別）** | HEGELOOP | revision_countはあるが、ラウンド別で差分・チェック状況を見る機能がない |
| 8 | **コンテンツテンプレートの完全UI（一覧/作成/編集/削除/テンプレから起票）** | HEGELOOP / シートPro | DBはあるが管理UIのフローが不完全 |

### P2 — 後でも良いがmoatになる機能

| # | 機能 | 対象競合 | 現状 |
|---|---|---|---|
| 9 | **Slack/Discord通知（メッセージ送信）** | シートPro | フィールドあり。AIチャットとは別に「ステータス変更をSlackに流す」機能 |
| 10 | **担当者別リソース/負荷可視化** | シートPro | workload_pointsはあるが人別集計チャートなし |
| 11 | **スプレッドシート的インライン編集** | シートPro | 一覧行展開編集はあるがセル直接編集はない |
| 12 | **バックアップ/復元フロー** | シートPro | exportはあるが、自動バックアップ・ポイントインタイム復元なし |
| 13 | **Lark連携** | HEGELOOP | コードゼロ。グローバル展開時に必要 |
| 14 | **グラフ可視化ライブラリ（Chart.js等）** | シートPro | 数値は計算済み。視覚化のみ未着手 |

---

## 6. すぐ着手すべき実装タスク（Top 10）

| # | タスク名 | なぜ必要か | 対象競合 | 着手ファイル |
|---|---|---|---|---|
| 1 | **ChatWork API統合（トークン保存・ルーム送信）** | SNS運用代行の現場ではChatWorkがデファクト。これなしでHEGELOOPから置換できない | HEGELOOP / シートPro | `app/settings/workspace/page.tsx`にAPIトークン入力UI追加。`lib/chatwork.ts`を新規作成。`projects.chatwork_room_id`を活用した送信フック実装 |
| 2 | **カスタムロールのRLS実適用** | UIで設定した権限がDBで機能せず。税理士・外部ディレクターのアクセス制御が不可能 | 全競合 | `supabase/sql/`に新規migrationを追加。`org_roles.permissions` JSONBを読むRLS Policy関数を実装 |
| 3 | **動画レビュー基盤（コンテンツ単位コメント + URL付き修正指示）** | HEGELOOPの核心機能。タイムコードまで不要でも「コンテンツ単位の修正指示＋チェック消込」は必須 | HEGELOOP | `app/contents/[id]/`ディレクトリを新規作成。`page_comments`テーブルをベースにコンテンツ用コメントシステムを実装 |
| 4 | **定時自動リマインド（Vercel Cron接続）** | `app/api/notifications/digest/route.ts`が実装済みだが呼ばれていない | HEGELOOP / シートPro | `vercel.json`にcrons設定追加。`app/api/cron/daily-digest/route.ts`を新規作成し既存digestロジックを呼び出す |
| 5 | **修正ラウンド管理UI（初稿→修正N）** | revision_countは集計されているが、ラウンド別の差分・チェック状況確認機能がない | HEGELOOP | `app/contents/page.tsx`の行展開部に修正ラウンドタブ追加。`supabase/sql/`にrevision_roundsテーブルのmigration追加 |
| 6 | **コンテンツテンプレート管理の完全UI** | `content_templates`テーブルは存在するが、一覧/作成/編集/削除/テンプレから案件起票の完全なUIフローがない | HEGELOOP / シートPro | `app/settings/templates/page.tsx`を新規作成。`app/api/content-templates/*`ルート追加 |
| 7 | **freee互換CSV出力（または正式全銀フォーマット）** | 経理連携の訴求に必須。"zengin_simple"の正式対応確認・追加 | bill change | `app/api/payouts/csv-export/route.ts`のフォーマット分岐を拡張。freeeインポート形式CSVの仕様実装 |
| 8 | **PWA manifest.json追加** | モバイルホーム画面への追加 + ネイティブアプリ感のための最低限対応 | HEGELOOP | `public/manifest.json`を新規作成。`app/layout.tsx`の`<head>`にlinkタグ追加 |
| 9 | **担当者別負荷可視化（workload集計ビュー）** | `project_tasks.workload_points`は既にDBにある。集計ビューのみ追加 | シートPro | `app/timeline/page.tsx`にアサイニー別の工数集計パネル追加。既存Supabaseクエリを拡張 |
| 10 | **グラフライブラリ導入（finance-liteの可視化強化）** | 数値計算は完了済み。Chart.js/Rechartsを追加するだけでシートProのビジュアル優位性を潰せる | シートPro | `package.json`にrecharts追加。`app/finance-lite/page.tsx`の数値表示をグラフに置換 |

---

## 7. 実装確認の根拠メモ

### 読んだ重要ファイル
- `app/contents/page.tsx` — フル実装確認（健康スコア、複合フィルタ、一括操作）
- `app/billing/page.tsx` — フル実装確認（月次バッチ生成、プレビュー、PDF）
- `app/payouts/page.tsx` — フル実装確認（全銀CSV出力、PDF ZIP、状態管理）
- `app/invoices/page.tsx` — フル実装確認（一括ZIP、送信準備）
- `app/calendar/page.tsx` — フル実装確認（ICS、Google Calendar連携）
- `app/timeline/page.tsx` — フル実装確認（ガント、依存関係）
- `app/finance-lite/page.tsx` — フル実装確認（粗利、低マージン検知）
- `lib/monthlyBilling.ts` — フル実装確認
- `lib/contentWorkflow.ts` — フル実装確認（13ステータス、ヘルススコア計算）
- `lib/projectWorkspace.ts` — フル実装確認（RuntimeException生成ロジック）
- `lib/notifications.ts` — フル実装確認（20通知種別）
- `lib/auditLog.ts` — フル実装確認（36アクション種別）
- `app/api/billing/generate/route.ts` — フル実装確認（請求番号採番含む）
- `app/api/payouts/csv-export/route.ts` — フル実装確認
- `app/api/invoices/bulk-zip/route.ts` — フル実装確認（Puppeteer生成）
- `supabase/sql/001_schema.sql` 〜 `062_platform_creator_license_v2.sql` — 62ファイル確認
- `app/settings/roles/page.tsx` — 「**Custom roles are UI-only, RLS still uses owner/executive_assistant**」コメント確認（致命的)
- `components/AIPalette.tsx` — 15モード確認（ChatWork送信は生成のみ、実送信なし）

### 仕様書にはあるが実装されていない可能性が高い箇所
1. **freee連携** — `AGENTS.md`・`docs/spec.md`・`docs/freee-csv.md`に記述あり。実装コードゼロ。
2. **ChatWork連携** — `projects.chatwork_room_id`フィールドと設計ドキュメントあり。APIクライアントゼロ。
3. **Lark連携** — 設計文書で言及あるがコードベース上に文字列すらなし。
4. **全銀正式フォーマット** — `zengin_simple`という独自フォーマットを実装。金融機関が受け付ける固定長全銀フォーマットかどうかは未検証。

### コードはあるが未接続っぽい箇所
1. **`app/api/notifications/digest/route.ts`** — digestAPIが存在するが、Vercel CronやEdge Functionからの自動呼び出し設定が見当たらない。手動/外部トリガーのみ。
2. **`export_jobs`テーブル + `/api/exports/*`** — ジョブキューは設計されているが、非同期バックエンドワーカー（Queue処理）が確認できない。大量エクスポート時にタイムアウトする可能性。
3. **`org_roles.permissions` JSONB** — フロント側でcanViewFinance等のフラグを参照しているが、SupabaseのRLS Policyからは参照されておらず、DBレベルの制御として機能していない。
4. **`projects.chatwork_room_id` / `slack_channel_id` / `discord_channel_id`** — フィールドは存在し、例外検知(`integration_missing`)でも参照されているが、実際に送信するロジックがない。
5. **`app/api/invoice-requests/reminders/route.ts`** — リマインダーAPIが存在するが、Cronからの自動起動設定未確認。